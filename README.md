```sh
npm install
npm run setup:emscripten              # or set EMXX to an existing em++
npm run dev -- --folder <PATH_TO_PLATFORMIO_PROJECT>  # or --sketch <PATH_TO_ARDUINO_SKETCH>
```
access at http://localhost:5173
